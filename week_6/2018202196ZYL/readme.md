期中考大作业基本思路：
先根据输入判断是否需要继续未完成的任务（调用缓存）。
如果否，则清空三个文档（已爬取的url，爬取异常的url以及存在set中的所有url）。重新开始爬取（使用bfs），并利用之前的HTML parser中解析url的函数，加之识别是否符合爬取条件，并在过程中义一个网页为单位向三个文档中分别存储对应数据以便断点重查。
如果是，则读取三个文档并将其中的数据分别存到对应的容器中。之后步骤同上。